using UnityEngine;

public class ClothingFitOffset : MonoBehaviour
{
    // Per-garment fine-tuning after snapping to a slot.
    public Vector3 localPositionOffset = Vector3.zero;
    public Vector3 localEulerOffset    = Vector3.zero;
}
